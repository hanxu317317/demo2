'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = createChainedFunction;
/**
 * 把多个方法组合成一个方法
 * const fun = createChainedFunction((a, b) => console.log(a + b),
 *                                   (a, b) => console.log(a - b))
 *
 * fun(3, 1)
 * >> 4
 * >> 2
 */

/* eslint-disable */
function createChainedFunction() {
    var _this = this;

    for (var _len = arguments.length, funs = Array(_len), _key = 0; _key < _len; _key++) {
        funs[_key] = arguments[_key];
    }

    return funs.filter(function (x) {
        return typeof x === 'function';
    }).reduce(function (acc, fn) {
        if (acc === null) {
            return fn;
        }
        return function () {
            for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
                args[_key2] = arguments[_key2];
            }

            acc.apply(_this, args);
            fn.apply(_this, args);
        };
    }, null);
}
module.exports = exports['default'];